# Not part of the app codebase — internal planning/reference material, keep out of git
reference/
session_handoff.md

# Ignore all markdown notes except README.md and the two files CLAUDE.md
# explicitly designates as pushed (CLAUDE.md itself, and PLAN.md).
*.md
!README.md
!CLAUDE.md
!PLAN.md

# Standard
node_modules/
.next/
dist/
build/
.env
.env.local
.DS_Store
*.log

# Playwright (dev-only smoke testing, see package.json)
test-results/
playwright-report/
blob-report/
playwright/.cache/
